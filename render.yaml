services:
  - type: web
    name: tiny-home-ai
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "cd tiny-home && gunicorn app:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: GROQ_API_KEY
        value: ""  # Set this in Render dashboard